defmodule SrpcClient.UserKey do
  # alias :srpc_lib, as: SrpcLib

  def agreement(_conn_info, _id, _password) do
    #   exchange = exchange()

    #   conn_state ++ []
  end

  # defp exchange do
  # end
end
